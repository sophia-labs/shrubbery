# PLANTER Slice — Final Design (synthesized)

**Date:** 2026-07-11 · **Worktree:** `$REPO` @ feat/planter-slice (b2f408e)
**Status:** FINAL — the build team executes this without further design work.
**Working name:** `planter` everywhere (placeholder — the real name is Vera's decree, R8).

---

## 0. Synthesis provenance

Three independent designs (oss-purist / hoist-pragmatist / hosted-first) were judged by three
independent judges who named **three different winners**. The consensus winner is computed over
their full rankings:

- Pairwise (Condorcet): **oss-purist beats hosted-first 2–1** (J2, J3 prefer it) and
  **beats hoist-pragmatist 2–1** (J1, J2 prefer it). Borda: oss-purist 4, hosted-first 3,
  hoist-pragmatist 2.

**Design 1 (oss-purist) is therefore the backbone**: its contract engineering (closed error
taxonomy, capability-flag/method agreement, conformance suite as a shippable artifact, the
oxigraph-WASM-backed SPARQL 1.1 Protocol proof, the browser-safety import-graph test, the
set-headers-once construction) survives intact. Every judge veto is honored — none is rebutted —
and every graft that does not conflict with a Quarterback ruling is applied. §10 records the
decisions, §12 (Appendix B) is the veto-compliance ledger, and inter-judge conflicts are resolved
explicitly in §10 (D2, D4, D5, D9, D16, D17).

Ground truth: the nine WF-A scout reports; all claims below were re-verified against the tree
this session where load-bearing (emporium import lines, StoreStatus members, vocab-views imports,
the kind-stamping fork, `asOf` lens docs, nucleus exports).

---

## 1. The TripleSource sub-contract (R1, R2)

**Location:** new file `packages/nucleus/src/triple-source.ts` — a **standalone sibling** of
`ShrubberyContract`, never a member of it (`ShrubberyContract` now requires `wireMode`/`ui`/
`runtime`; a read-only host must not fake them — a stubbed contract is a mock). Exported from
`packages/nucleus/src/index.ts` **only** (no new package.json subpath — `packages/nucleus/
package.json` has a single owner, the kind-reconciliation unit U2; see D14). Nucleus outside
`wire-mode-controller.ts` is verified unclaimed by all active branches (collision-map).

The contract speaks only RDF universals: named-graph IRIs, the nucleus `Triple`/`Term` model,
N-Triples text, epoch-ms clocks, SPARQL as an *optional declared capability*. MCP envelopes,
graphId conventions, gateway routes, auth unions, loopback manifests are **adapter construction
parameters**, invisible behind the interface. The third-party test: a stranger with a plain
Oxigraph/Fuseki endpoint implements this and passes `@shrubbery/source/conformance` without ever
hearing the word "gardend".

### 1.1 Final interface source

```ts
/**
 * triple-source.ts — the TripleSource sub-contract: reading RDF as testimony.
 *
 * A STANDALONE sibling of ShrubberyContract (contract.ts), NOT a member of it:
 * a read-only host must be able to consume a triple store without faking
 * wireMode/ui/runtime. This file speaks only RDF universals — named-graph
 * IRIs, the nucleus Triple/Term model, N-Triples text, epoch-ms clocks, and
 * (optionally) SPARQL. Anything store-specific — MCP envelopes, loopback
 * manifests, gateway routes, graphId conventions, credentials — lives in the
 * adapter behind a factory, never in this interface. A stranger with a plain
 * Oxigraph/Fuseki endpoint can implement this contract without ever hearing
 * the word "gardend"; `@shrubbery/source/conformance` is the acceptance test.
 *
 * CONTRACT INVARIANTS (enforced by the conformance suite):
 *   1. Every read returns testimony: readAt (epoch ms), graphIri (what was
 *      actually read), tripleCount.
 *   2. At least one of `triples` / `nt` is present; when both are, they agree.
 *      `tripleCount` MUST equal the parsed triple count — never a transport
 *      envelope's own counter (the gardend `quadCount` lie).
 *   3. EMPTY (tripleCount === 0) is a successful read — a first-class store
 *      state, never an error and never an excuse for a fallback body.
 *      (parseTriplesToConfig throws on empty input; hosts pre-check.)
 *   4. Capability flags and optional methods agree: `select` is implemented
 *      iff `description.sparql`; `subscribe` is implemented iff
 *      `description.liveness === 'push'`. Hosts discover capabilities by
 *      reading `description`, never by probe-and-catch.
 *   5. Failures are thrown ONLY as TripleSourceError with a code from the
 *      closed taxonomy below; adapters map their transport's errors and carry
 *      the upstream message verbatim.
 *   6. `close()` is idempotent and releases every socket/timer/process the
 *      source owns.
 *   7. LIVENESS IS TESTIMONY, NOT ASPIRATION. A 'static' source's readAt is
 *      the fossil's CAPTURE time; an adapter must REFUSE construction rather
 *      than fabricate it (no-mocks applies to timestamps). A live source's
 *      readAt is read-completion time (the CellConfigRead.readAt lineage).
 *
 * Pure types + tiny pure helpers only — no DOM, no network, no node builtins.
 */

import type { Term, Triple } from './workspace/rdf-model.js'
import { parseNT } from './workspace/rdf-model.js'

// ── Liveness ─────────────────────────────────────────────────────────────────

/**
 * How this source learns about change — DECLARED by the adapter, never
 * inferred by the host:
 *   'push'   — the source notifies via subscribe(). NO in-tree adapter can
 *              honestly declare this today (gardend exposes no RDF named-graph
 *              change feed; the only SSE is service logs; hocuspocus carries
 *              CRDT sync, not :ux:config). The seam is reserved for one that
 *              can.
 *   'poll'   — reads are live but change detection is the host's re-read loop
 *              (honor `suggestedPollMs` when present).
 *   'static' — the source is a fossil; content can never change and readAt is
 *              the fossil's CAPTURE time (invariant 7), not load time.
 */
export const SOURCE_LIVENESS = ['push', 'poll', 'static'] as const
export type SourceLiveness = (typeof SOURCE_LIVENESS)[number]

// ── Self-description (capability discovery) ─────────────────────────────────

/** A source's self-description. Safe to serialize and show to users/agents —
 *  MUST NOT contain credentials (endpoints are redacted of tokens/userinfo by
 *  the adapter). Adapters may structurally extend this with adapter-specific
 *  display fields (e.g. the hosted adapter's readPath) — such fields live in
 *  the adapter package, never here. */
export interface TripleSourceDescription {
  /** Adapter identity, e.g. 'gardend-local' | 'hosted-gateway' | 'sparql-http'
   *  | 'static-nt' — or any third-party name. OPEN string; display/diagnostic
   *  only: hosts MUST NOT switch behavior on it. */
  readonly kind: string
  readonly liveness: SourceLiveness
  /** True iff select() is implemented. */
  readonly sparql: boolean
  /** Optional provenance for testimony display (endpoint URL, file path…).
   *  Never a secret. */
  readonly endpoint?: string
  /** For 'poll' sources: the adapter's recommended re-read interval. */
  readonly suggestedPollMs?: number
}

// ── The read result: testimony, not just data ───────────────────────────────

/** One read of one named graph, with its provenance. */
export interface TripleRead {
  /** The named-graph IRI that was actually read. */
  readonly graphIri: string
  /** Epoch ms at which these triples were obtained FROM THE AUTHORITATIVE
   *  STORE: read completion for live sources, capture time for fossils. */
  readonly readAt: number
  /** Authoritative count — equals the parsed triple count (invariant 2). */
  readonly tripleCount: number
  /** Parsed triples (preferred carrier). */
  readonly triples?: readonly Triple[]
  /** Raw N-Triples body (alternate carrier; present when the source natively
   *  produced one — rdf_dump bodies, fossils. Useful for the turtle face,
   *  debugging, re-serialization). */
  readonly nt?: string
}

/** EMPTY is a first-class state — sugar for the pre-parse check every host
 *  must make before parseTriplesToConfig. */
export function isEmptyRead(read: TripleRead): boolean {
  return read.tripleCount === 0
}

/** Normalize a read to triples: prefer the parsed carrier, else parse `nt`.
 *  Throws TripleSourceError('protocol') if the read carries neither. */
export function triplesOf(read: TripleRead): readonly Triple[] {
  if (read.triples) return read.triples
  if (read.nt !== undefined) return parseNT(read.nt)
  throw new TripleSourceError(
    'protocol',
    `TripleRead for <${read.graphIri}> carries neither triples nor nt`,
  )
}

// ── Optional SPARQL capability ───────────────────────────────────────────────

/** A SELECT binding term. Extends the nucleus Term with blank nodes, which
 *  third-party stores legitimately return even though the nucleus Triple
 *  model (deliberately) never emits them. */
export type SourceTerm = Term | { readonly type: 'bnode'; readonly value: string }

/** A SPARQL SELECT result, with the same testimony discipline as reads. */
export interface SelectResult {
  readonly rows: ReadonlyArray<Readonly<Record<string, SourceTerm>>>
  readonly readAt: number
}

// ── Error taxonomy ───────────────────────────────────────────────────────────

export type TripleSourceErrorCode =
  | 'unauthorized' // credentials missing/expired (HTTP 401-shaped)
  | 'forbidden'    // authenticated but not allowed (403-shaped)
  | 'not-found'    // store/dataset/graph does not exist (404-shaped)
  | 'unavailable'  // store unreachable / still starting / 5xx-shaped
  | 'protocol'     // the store answered, but not in the contracted shape

/** The ONLY failure shape adapters may throw. `detail` carries the upstream
 *  message verbatim (honest errors, never rewritten). */
export class TripleSourceError extends Error {
  readonly code: TripleSourceErrorCode
  /** Upstream HTTP status, when there was one. */
  readonly status?: number
  readonly detail?: string

  constructor(
    code: TripleSourceErrorCode,
    message: string,
    opts?: { status?: number; detail?: string; cause?: unknown },
  ) {
    super(message, opts?.cause !== undefined ? { cause: opts.cause } : undefined)
    this.name = 'TripleSourceError'
    this.code = code
    this.status = opts?.status
    this.detail = opts?.detail
  }
}

// ── The contract ─────────────────────────────────────────────────────────────

/**
 * A readable RDF store bound (at construction, by an adapter factory) to one
 * endpoint + dataset + credential. `read` addresses named graphs WITHIN that
 * binding — which is what keeps store-specific graph addressing (e.g. a
 * gardend graphId) out of this interface.
 */
export interface TripleSource {
  readonly description: TripleSourceDescription

  /**
   * Dump one named graph. EMPTY (tripleCount === 0) resolves successfully.
   * Rejects only with TripleSourceError.
   */
  read(graphIri: string): Promise<TripleRead>

  /**
   * OPTIONAL — present iff description.sparql. Run a SPARQL SELECT against
   * the bound store (the read-recipe's COUNT cross-check; FID-004 probes;
   * richer faces).
   */
  select?(sparql: string): Promise<SelectResult>

  /**
   * OPTIONAL — present iff description.liveness === 'push'. Reserved seam:
   * NO in-tree adapter implements it this slice (no RDF push surface exists
   * on gardend). Returns an unsubscribe function.
   */
  subscribe?(graphIri: string, onChange: (read: TripleRead) => void): () => void

  /** Release sockets/timers/processes. Idempotent. */
  close(): Promise<void>
}
```

Notes vs the submitted designs:
- The `'static'` liveness doc and invariant 7 adopt **Design 3's fossil doctrine** (unanimous
  graft; D1's "readAt may advance" and D2's "readAt = load time" are vetoed freshness-laundering).
- `select?` keeps **Design 1's portable `SelectResult`/`SourceTerm` shape** (J2's veto of D2's
  raw-string `SparqlRows`); the gardend/gateway adapters map the proven `{rows}` envelope via
  `parseTerm` *inside* the adapter, which is what J1's graft actually needs (in-contract COUNT
  cross-checks with a shape the existing clients satisfy mechanically). See D4.
- `description.kind` stays an **open string** (J2's veto of D2's closed SourceKind union).
- The hosted adapter's `readPath` display field lives on a **package-level description subtype**
  (`HostedGatewaySourceDescription extends TripleSourceDescription`) in `@shrubbery/source`,
  keeping the nucleus contract free of gateway-isms while `/source` can still state the trust
  path. See D6.

### 1.2 The store envelope — first-class EMPTY without touching nucleus StoreStatus

Canonical `StoreStatus` (`'idle'|'loading'|'ready'|'error'`, reactive-store.ts:34) is
**load-bearing and frozen-consumed** (its doc comment says members are string-compared; runtime is
the 7-branch contention zone). It is NOT widened. Instead `packages/source/src/store/
source-store.ts` defines the package-local envelope, reusing the canonical member names:

```ts
export type SourceStoreStatus = 'idle' | 'loading' | 'ready' | 'empty' | 'error'
export interface SourceState {
  readonly status: SourceStoreStatus
  readonly read: TripleRead | null        // last completed read testimony (set for ready AND empty;
                                          //   retained through later errors, labeled stale by hosts)
  readonly config: WorkspaceConfig | null // non-null iff status === 'ready'
  readonly error: string | null           // verbatim message, never softened
  readonly errorKind: TripleSourceErrorCode | 'parse' | null
}
```

`createConfigStore(source, graphIri, { pollMs? })` implements `refresh()` + `startPoll()`
(session-store lineage), pre-checks `tripleCount === 0` → `'empty'`, and classifies errors
**1:1 from the contract taxonomy** (`TripleSourceError.code`), plus `'parse'` for a
`parseTriplesToConfig` throw on non-empty input — never by sniffing transport error shapes
(J1 graft). No fallback config exists anywhere in the tree. Whether `'empty'` is eventually
absorbed into the canonical nucleus `StoreStatus` is ledgered for when the runtime freeze lifts
(§8 strangle ledger), not decided here.

---

## 2. `packages/source` — the one hoisted adapters package (`@shrubbery/source`) (R3)

**Name:** `packages/source` (designer's call, endorsed by all three designs): the package's
charter is "where triples come from"; a stranger's Fuseki is not a cell, but it is a triple
source.

### 2.1 Layout

```
packages/source/
  package.json               # "@shrubbery/source"; exports "." (browser-safe), "./node",
                             #   "./emporium", "./conformance". Deps of ".": @shrubbery/nucleus ONLY.
  tsconfig.json
  src/
    index.ts                 # browser-safe barrel: adapters (browser-reachable), boot, auth,
                             #   store, errors, fossil codec. Enforced by the browser-safety test.
    boot.ts                  # PlanterBootConfig {endpoint, graph, auth} + auth union (R8)
                             #   + sourceFromBoot (explicit-adapter-wins, refuse-ambiguous — §2.4)
    auth.ts                  # createAuthProvider(auth): nucleus AuthProvider —
                             #   'dev'→StaticAuth, 'cognito'→CognitoAuthSession, 'none'→Anonymous
    transport/
      mcp-client.ts          # THE unified McpClient. Base = apps/organism/src/cell/loopback-mcp.ts
                             #   (byte-identical org/atelier copy) — preserves the Node-25
                             #   undici-drops-Authorization node:http fallback VERBATIM and the
                             #   browser same-origin /cell proxy split. UPGRADED with rhizome's
                             #   structuredContent-first result handling (garden-client.ts:145-165).
                             #   One transport config: { mcpUrl, token?, origin?, fetch?,
                             #   transport?: 'auto'|'fetch'|'node-http' } (unifies LoopbackTransport
                             #   / EmporiumTransport / GardenClientConfig).
      parse-term.ts          # rhizome parseTerm (garden-client.ts:60-92) → SourceTerm
      wait-healthy.ts        # rhizome waitHealthy(baseUrl, {timeoutMs, intervalMs})
    fossil/
      fossil-codec.ts        # fossil v1 provenance-header codec (parse + serialize; browser-safe)
      fossil-source.ts       # StaticNtSource — §2.6. liveness 'static'.
    gardend/
      gardend-source.ts      # GardendLocalSource — §2.5. liveness 'poll'.
    gateway/
      gateway-transport.ts   # hoist near-as-is (organism; per-request auth.whenReady()+token();
                             #   GatewayHttpError preserved as the taxonomy carrier)
      cognito-auth-session.ts# hoist near-as-is (organism; 491L, injectable fetch/storage/timers)
      gateway-source.ts      # HostedGatewaySource — §2.7. liveness 'poll'.
    sparql/
      sparql-source.ts       # generic SPARQL 1.1 Protocol adapter — §2.8. liveness 'poll'.
    store/
      source-store.ts        # SourceState + createConfigStore (§1.2)
    emporium/
      client.ts, store.ts, index.ts   # emporium migration home (§5). NOT vocab-views (→ app).
    conformance/
      suite.ts               # runTripleSourceConformance(name, makeSource, opts) — §2.3
      sparql-protocol-server.ts # real-oxigraph-WASM-backed SPARQL 1.1 Protocol HTTP server — §2.8
  src/node/                  # node-only subpath "@shrubbery/source/node" (explicit, visible split)
    index.ts
    spawn-gardend.ts         # organism copy (KEEPS the cwd:profileDir fastembed fix) +
                             #   loopback.json manifest discovery
    seed.ts                  # createGraphAndSeedUxConfig (create_graph snake_case →
                             #   rdf_load camelCase targetGraphIri) + seedGraphFromNtFile
                             #   (loopback AND gateway /g/{id}/mcp variants)
    fossil-io.ts             # readFossil(path)/writeFossil(path, read, source) over fossil-codec
  tests/                     # real-infra tests (§9)
```

**Export conditions.** `"."` is browser-safe — no static `node:*` imports; the McpClient keeps
the proven `isNode` check → dynamic `import('node:http')` pattern. The split is enforced by the
**browser-safety test** (J1 graft): it walks the import graph of the `"."` barrel and proves no
node builtins leak. `"./node"` is documented node-only. `pnpm-workspace.yaml` already globs
`packages/*` — no root workspace edit needed.

### 2.2 Hoist provenance (R3 — exact, and what is deliberately NOT copied)

| Hoisted module | Base | Folded contributions |
|---|---|---|
| `transport/mcp-client.ts` | organism `loopback-mcp.ts` | rhizome structuredContent-first results; unified transport config |
| `transport/parse-term.ts` | rhizome `garden-client.ts:60-92` | as-is |
| `transport/wait-healthy.ts` | rhizome `garden-client.ts:135-142` | as-is |
| `gateway/gateway-transport.ts` | organism `gateway-transport.ts` | as-is |
| `gateway/cognito-auth-session.ts` | organism `cognito-auth-session.ts` | as-is |
| `gardend/gardend-source.ts` | organism `gardend-contract.ts` LoopbackRestClient read path | **atelier's contribution**: the generic `dump(graphId, sourceGraphIri)` primitive (hosted shape, hosted-gateway-contract.ts:89-108) expressed on the loopback path — `read(graphIri)` subsumes dumpUxConfig AND the `:ux:control` read |
| `fossil/fossil-source.ts` | modeled on organism harness `in-memory-cell-contract.ts` | reduced to the TripleSource surface + D3 fossil doctrine |
| `store/source-store.ts` | organism `session-store.ts` (`CellConfigRead.readAt` is `TripleRead.readAt`'s direct ancestor) | EMPTY pre-check + taxonomy classification |
| `node/spawn-gardend.ts`, `node/seed.ts` | organism `spawn-gardend.ts` (newer copy, cwd fix) | seed split out; gateway seeding variant |
| `emporium/client.ts`, `emporium/store.ts` | organism `emporium-client.ts` / `emporium-store.ts` | as-is (§5) |

Every hoisted file carries a provenance header: `HOISTED from apps/organism/src/cell/<file> @
b2f408e — organism's copy is frozen under active swarm ownership; @shrubbery/source is the sole
external adapter-consumption convention. Organism's migration is a named follow-up slice.`

**Deliberately NOT copied** (vetoed by all three judges): the WireWriter, both CRDT backends
(gateway/loopback), the full `GardendContract`/`HostedGatewayContract` assemblies, app-routes,
public-shell, session-store's full contract machinery. Planter is a read-only host; that is
write-path and shell capability with zero in-slice consumers, and it is exactly the drift surface
that rotted atelier's fork. The one WireWriter doctrine worth keeping — hosted echo-parse +
throw-on-missing-wire-id must win over loopback's `?? ''` fallback — is **recorded in the strangle
ledger** (`docs/planter/adapters.md`), not implemented (J2 graft).

Atelier's app-local `src/cell/` fork is not copied and not migrated this slice (it is red on an
unrelated artifact-PUT; its migration rides the organism follow-up). Its one unique capability
(the `:ux:control` read) is preserved because `read(graphIri)` is generic over sibling graphs.

### 2.3 The conformance suite — the shippable acceptance kit (all-judges graft)

`@shrubbery/source/conformance` exports `runTripleSourceConformance(name, makeSource, opts)`,
parameterized over a source factory, run in-repo against **all four adapters on real
infrastructure**, and shipped as the third-party implementer's acceptance test. It encodes the
R2 invariants once:

- testimony fields present and self-consistent; `tripleCount` === parsed length (=== SELECT COUNT
  when sparql-capable);
- EMPTY graph resolves with 0 — never throws, never falls back;
- declared liveness ∈ `SOURCE_LIVENESS`; `subscribe` present ⇔ `'push'`; `select` present ⇔
  `description.sparql`;
- failures thrown only as `TripleSourceError` with upstream detail verbatim;
- `close()` idempotent;
- `description` serializable and credential-free (greppable: no token/secret substrings).

### 2.4 Boot config and the source factory (R8)

```ts
export type PlanterAuth =
  | { mode: 'dev'; token: string }
  | { mode: 'cognito'; region: string; clientId: string; userPoolId?: string }
  | { mode: 'none' }

export interface PlanterBootConfig {
  endpoint: string          // gateway base | loopback base or '/cell' | SPARQL URL | 'static:<path-or-url>'
  graph: string
  auth: PlanterAuth
  adapter?: 'gardend-local' | 'hosted-gateway' | 'sparql' | 'static-nt'  // explicit ALWAYS wins
  readPath?: 'mcp' | 'sparql'   // hosted only; DEFAULT 'sparql' (§2.7)
  configGraphIri?: string       // override for non-Mnemosyne stores; default uxConfigGraphIri(graph)
  pollMs?: number               // default: adapter's suggestedPollMs; 'static' ignores it
}
```

`sourceFromBoot(config)` (Design 3's refusal semantics, grafted by J2+J3; D2's URI-scheme
grammar is vetoed): the explicit `adapter` field **always wins**; exactly two inference rules
exist — `endpoint` starting `static:` → `static-nt`, and `auth.mode === 'cognito'` →
`hosted-gateway` — and any other adapter-less config is **refused with a config error naming the
`adapter` field**. A wrong guess against real infrastructure is a lie; `auth.mode:'none'` against
the gateway produces an honest 401 → `unauthorized`, never a silent downgrade.

### 2.5 gardend-local adapter

`createGardendLocalSource({ mcpUrl | manifest, token, graphId, suggestedPollMs? })` over the
unified McpClient. `read(graphIri)` = `tools/call rdf_dump { graphId, format:
'application/n-triples', sourceGraphIri: graphIri }` → `parseNT` → `TripleRead` with native `nt`;
generic over sibling graphs (`:ux:config`, `:ux:control`, …). `select(sparql)` = `sparql_query`
→ `{rows}` → `parseTerm` → `SourceTerm` bindings. `description = { kind: 'gardend-local',
liveness: 'poll', sparql: true, endpoint }`. Errors mapped: connect failure → `unavailable`,
401 → `unauthorized`, McpError → `protocol` (message verbatim).

### 2.6 static-nt fossil adapter (Design 3 doctrine — mandatory graft)

`staticNtSource(body, provenance)` where provenance is EITHER the parsed **fossil v1 header** or
an explicit `{ graphIri, capturedAt }`. Construction **without capture provenance is refused**
with a named error — no-mocks applies to timestamps; stamping load time launders a fossil's age
into apparent freshness (the exact W-1b defect). `read()` returns the construction-time parse
with `readAt = capturedAt`. `select` absent (honest absence). Fossil format, written by
`dump.mts`/`writeFossil` and parsed by `fossil-codec.ts`:

```
# shrubbery fossil v1
# graphIri: urn:mnemosyne:local:graph:g1:ux:config
# capturedAt: 1783200000000 (2026-07-11T19:20:00.000Z)
# source: gardend-local http://127.0.0.1:7090
<subject> <predicate> <object> .
…
```

`#` lines are skipped by `parseNT`, so the body remains a valid `.nt` file everywhere.

### 2.7 hosted-gateway adapter — the Viewer read path (R6)

Verified gateway facts (gateway-auth scout, platform-next authz.rs): GET/HEAD/OPTIONS on
`/g/{id}` need Viewer; POST is Viewer-eligible only on the exact allowlist including
`/api/sparql/query` (authz.rs:37); **POST `/g/{id}/mcp` uniformly requires Editor today** (the
SEAM comment at authz.rs:68-72); anonymous gets `/health` + signed image URLs only and never
spawns cells.

`createHostedGatewaySource({ endpoint, graph, auth, readPath = 'sparql', cellQueryRoute? })`:

- **`readPath: 'sparql'` — THE DEFAULT** (least-privilege ruling, J1 rationale + J3 graft):
  `POST /g/{id}{cellQueryRoute}` with `SELECT ?s ?p ?o WHERE { GRAPH <iri> { ?s ?p ?o } }`,
  rows reassembled to `Triple[]` via `parseTerm`. Viewer-eligible — the reader/published-site
  path. `cellQueryRoute` defaults `'/api/sparql/query'` with `'/api/graphs/query'` as the
  documented fallback: the default is verified only on the **gateway allowlist**, not the cell
  side (Design 3's catch, praised by J1) — the integration test probes which route the current
  gardend actually serves and pins it (open item OQ-1 resolves in-test).
- **`readPath: 'mcp'` — explicit opt-in**: `rdf_dump`/`sparql_query` via `POST /g/{id}/mcp`
  toolsCall. Richest read (native `nt`, true named-graph dump) but costs **Editor** today. For
  authoring/operator deployments that knowingly hold an Editor credential.

Every deployment shape (server, SPA, future static build) defaults `'sparql'`; escalation to
`'mcp'` is always an explicit boot-config act. `describe()` returns the package-level
`HostedGatewaySourceDescription` carrying `readPath`, so every face states which trust path
produced its bytes. Errors: `GatewayHttpError` 401→`unauthorized`, 403→`forbidden`,
404→`not-found` (gateway leaks no existence to non-members), 5xx/CellUnavailable/ECONNREFUSED→
`unavailable`, envelope-shape surprises→`protocol` — all with the gateway body verbatim in
`detail`.

**The recorded SEAM** (→ `docs/planter/eschaton-gateway-read-seam-memo.md`, Design 3's content,
grafted by all judges): (a) tool-level authz on `/g/{id}/mcp` (authz.rs:68-72, verbatim-cited) so
read-only tools become Viewer-eligible; (b) an anonymous-Viewer grant class for flagged public
graphs (denial-of-wallet guard rethought — pre-warmed/cached cells); (c) `PN_CORS_ORIGINS` must
include CDN/SPA origins (defaults empty = CORS layer disabled); (d) a cacheable GET read route
(ETag = content sha) for CloudFront; plus (e) the WP5.1 seeding gap (`ux_seed.rs` has zero
callers and its 4-triple default would seed a zero-region config).

**Proof tiers:** conformance in BOTH readPaths against real gardend behind the local
path-rewriter (existing hosted-test precedent, honest header disclaiming Cognito/ACL/ALB
coverage); an env-gated P2.5 run (`PLANTER_GATEWAY_URL` + `pn-dev-token` against a local
docker-desktop gateway) that `ctx.skip`s honestly when absent; the docker-desktop runbook ships
in docs (§8). **Never represented as cloud-proven.**

### 2.8 generic SPARQL adapter + the protocol reference server (Design 1, all-judges graft)

`sparqlSource({ endpoint, graphIri?, headers? })` speaks **pure SPARQL 1.1 Protocol** — zero
Sophia-isms (greppable purity check in its test: no gardend/gateway/MCP references). `read` =
SELECT-spo within `GRAPH <iri>` accepting standard `application/sparql-results+json` bindings
(including `bnode`); `select` passes queries through. liveness `'poll'`.

Its conformance target is `conformance/sparql-protocol-server.ts`: a ~120-line real HTTP server
backed by the **real oxigraph WASM engine** (devDependency) — a genuine non-gardend engine behind
genuine HTTP, deterministic post-install, no external daemon, no-mocks compliant. This is the
only way "generic" is *proven* rather than asserted, and it doubles as the third-party reference
target. A second, env-gated run targets any external endpoint (`PLANTER_SPARQL_URL`), skipping
honestly when unset. A codec-parity test pins term-for-term equality between the generic
adapter's SELECT-spo reassembly (oxigraph roundtrip of the 227-triple seed) and the fossil parse
of the same bytes.

---

## 3. `apps/planter` — the generic host

Boot resolution priority: URL query params (dev ergonomics) → `/planter.config.json` (same-origin
fetch; the CDN deployment carries a config next to the bundle) → env vars (`PLANTER_ENDPOINT`,
`PLANTER_GRAPH`, `PLANTER_AUTH_MODE`, … for the server). Both runnables share `sourceFromBoot`.

### 3.1 planter-server — the four faces (R5)

`apps/planter/src/server.ts`, node:http, modeled on `apps/rhizome/src/server.ts` **wholesale**:
`negotiate` (hoisted, §3.2) → **resolve the resource LIVE per-request from the TripleSource** →
`linksFor` once → render face. No snapshots anywhere: static bytes enter only via the fossil
ADAPTER, declared `'static'`, with capture provenance — this kills the W-1a app-local-JSON
pattern structurally.

Routes (all with `.md/.ttl/.json/.jsonld/.html` pins + Accept negotiation, bare curl → markdown):

- `GET /` and `/site` — the site: `source.read(configGraphIri)` → `isEmptyRead` pre-check →
  `triplesOf` → `parseTriplesToConfig` → the **EXISTING `workspace` Resource kind**
  (target.ts WorkspaceResource; turtle via `serializeConfigToTriples`, so the round-trip
  `parseTurtle → parseTriplesToConfig` is a pinned test). **Zero new Resource kinds, zero
  packages/render switch-site touches** (D5 — J3 veto honored over J1's endorsement).
- `GET /source` — **app-level route, not a render kind**: JSON face = the
  `TripleSourceDescription` + latest read testimony (the agent-discoverable capability card,
  Design 1); markdown/html faces = htmlShell-wrapped description page. Its markdown face includes
  the `site:ContentSource` triples of the vocab draft as an inline turtle block — the vocab's
  first living instance.
- `GET /health` — 200 + JSON of the last read status. Never fakes upstream health.
- `?asof` is RESERVED in the grammar (negotiate already splits query params): when present, the
  face carries an explicit "temporal lens not available for this source (liveness=poll; no
  history index)" line rather than silently ignoring it. Dated lenses are a named later slice.

**Dom-face floor** (R5): `src/html-shell.ts` adapted from `apps/storybook/conneg/html-shell.ts`
(provenance header) — a plain string HTML wrapper around the rendered markdown face with the
Link set reflected as `<link>` tags and the testimony footer. Zero SSR entanglement;
rhizome-grade Lit SSR is explicitly a **named later slice**.

**Testimony on every face — set once, before face dispatch** (Design 1's construction, J1
graft; face-invariant by construction, not asserted per-route):

- Headers on ALL four faces: `X-Shrubbery-Read-At` (epoch ms), `X-Shrubbery-Read-At-Iso`,
  `X-Shrubbery-Graph-Iri`, `X-Shrubbery-Triple-Count`, `X-Shrubbery-Source` (kind),
  `X-Shrubbery-Liveness`, plus `Last-Modified`.
- Body blocks composed at the HOST seam (no render-package changes): a hypertext/html footer
  (`read live from {endpoint} at {formatTimestamp(readAt)} — {n} triples in <{graphIri}>
  ({liveness})`), and a `#`-comment capture block prefixed to the turtle face (J2 graft —
  comments are skipped by the parsers, so `parseTurtle → parseTriplesToConfig` round-trip is
  preserved and pinned by test). The JSON-LD body stays pure config this slice (headers carry its
  testimony; body-level parity is a ledgered follow-up).
- The tested Link-set-parity invariant in packages/render is untouched.

**Empty/error taxonomy** (shared classification in `src/read-status.ts`, consumed by server and
SPA):

| condition | detection | server | SPA |
|---|---|---|---|
| EMPTY | settled read, `tripleCount === 0` | **200** honest empty page per face + `X-Shrubbery-Triple-Count: 0` | `'empty'` panel: graphIri, readAt, "0 triples — seed with `pnpm --dir apps/planter seed …`" (documentation affordance, never an auto-write) |
| unauthorized / forbidden | `TripleSourceError` 401/403-shaped | 401/403 passthrough, upstream body verbatim | `'error'` panel, verbatim + which auth arm |
| not-found | 404-shaped (no grant / unknown graph) | 404 face | verbatim |
| unavailable | connect failure / 5xx / CellUnavailable | **503** (+ Retry-After), body names endpoint + verbatim error | `'error'` panel + retry affordance |
| malformed | non-empty triples, `parseTriplesToConfig` throws | 500, verbatim parse error + pointer to `/site.ttl` (the raw triples ARE inspectable) | `'error'`, verbatim |

No fallback config exists anywhere. Error faces carry the last successful read's capture block
when one exists, clearly labeled stale.

### 3.2 negotiate hoist (R5)

The rhizome variant (path + target + `query: URLSearchParams`) moves to
`packages/render/src/negotiate.ts`, exported from the render index — pure string-in/decision-out
(the CloudFront-transcription property preserved), already importing only `RenderTarget`. The
unified `NegotiationResult` is the superset `{ path, target, query, pinned }` (rhizome's `query`
+ storybook's `pinned`) so both existing consumers migrate mechanically. **Both app copies are
retired** (J2 veto of leave-alive): `apps/rhizome/src/negotiate.ts` deleted + `server.ts`
repointed; `apps/storybook/conneg/negotiate.ts` deleted + `server.ts`/`build-static.ts`
repointed (`conneg/**` is NOT under the frozen `stories/**`). Behavior pinned byte-identical by
keeping both apps' existing negotiate tests running against the hoisted function. Exactly one
negotiate survives.

### 3.3 planter SPA

Vite app; `vite.config.ts` **consumes** `scripts/vite-cell-proxy.ts` `dynamicCellProxy` for the
gardend-local dev mode (never edits it; token never ships to the browser, manifest rotation
free). Flow: boot → `sourceFromBoot` → `createConfigStore` → `startPoll` → on `'ready'`:
`parseTriplesToConfig → planFor → renderWorkspace` (imported as-is from `@shrubbery/runtime` —
its store-free-island/inert-seams doctrine is exactly right for a read-only host; zero edits to
NO-TOUCH runtime files). On `'empty'`/`'error'`: honest panels per §3.1, stamped with the kind
register. A persistent **status strip** renders the source description + capture-age chip:

```html
<span class="mn-kind" data-kind="testimony">read {age} ago · {kind} · {liveness}</span>
```

— the first consumer proving kind.css lights up end-to-end (§4).

**Browser proof** (J3 graft): the fast tier is a vitest + happy-dom liveread test mirroring
`gardend-liveread.integration.test.ts` (6 regions / 7 panels from a real cell **through
`@shrubbery/source`** — the hoist's no-drift guard); the real-browser gate is a **planter-owned
Chromium runner** `apps/planter/scripts/planter-browser.mts` (the organism harness is NO-TOUCH)
driving the SPA through the real vite dev proxy against a real spawned gardend, asserting the
`.mn-kind[data-kind=testimony]` chip's computed style from kind.css, a poll-driven re-render
after a real `sparql_update`, and the honest error panel after killing the cell.

### 3.4 FID-004 gate (R10)

`packages/source/tests/fid004-convergence.probe.test.ts`, template commit 2ce2357: spawn real
gardend on a persistent temp profile → write `:ux:config` triples via real API (`rdf_load` AND a
`sparql_update` variant) → read back via `sparql_query` **cold** → SIGTERM → respawn on the same
profile → re-read both paths → assert count and term-for-term parity with `rdf_dump`. This unit
is a **structural `depends_on` edge** of both host units (U9, U11) — no live-read claim in any
doc, face, census note, or test name ships before it is green in this environment (J2 hard
veto). The green run (gardend binary sha + date) is recorded in `docs/planter/README.md`. If
red: escalate as a garden-side memo; host acceptance re-scopes to the fossil adapter, honestly
labeled.

---

## 4. Kind-vocabulary reconciliation (R4 — reconcile, never re-mint)

Canonical home stays `packages/nucleus/src/kinds/index.ts`. No `packages/kinds`. Three moves:

**(a) Cross-package equality test.** Nucleus kinds gains the runtime register
`export const DISPLAY_KINDS = ['identity','state','metric','prose','reference','testimony','affordance'] as const`
with a compile-time exhaustiveness check against `DisplayKind` (no new values, R4). New
`packages/nucleus/src/__tests__/kind-register-parity.test.ts` (tokens as **devDependency** of
nucleus — tokens stays dependency-free, no runtime import either direction, hex-agreement-test
precedent) asserts THREE-way set-equality: `DISPLAY_KINDS` ≡ `Object.keys(KIND_APPLICATIONS)`
(tokens) ≡ the `[data-kind=…]` selectors declared in `packages/tokens/css/kind.css` **read from
disk**. Drift in any of the three fails CI.

**(b) Stamping fork closed — the `data-kind` attribute convention wins.** kind.css only selects
`.mn-kind[data-kind=…]` (verified), greenhouse already stamps it, tokens' KIND_APPLICATIONS
documents it; render-dom's `mn-kind-{kind}` classes are the sole outlier and light nothing.
Changes: `packages/render/src/render-dom.ts` emits `class="mn-kind" data-kind="{kind}"` and drops
the per-kind discriminator classes — the structural child classes (`mn-kind-value`, `mn-kind-unit`,
`mn-kind-link`, `mn-kind-attribution`) are NOT kind discriminators and STAY (kind.css selects
them as descendants); `packages/tokens/src/index.ts` gains `applyKind(el, kind)` parallel to
`applyStance`, driven by KIND_APPLICATIONS, with its own test (J2/J3 graft — the register finally
has a programmatic applier). Verified blast radius: `mn-kind-{kind}` discriminators appear only
in render-dom, its test, and tokens' test — no frozen story or component asserts them. End-to-end
proof: the SPA Chromium gate asserts computed style on real rendered output (§3.3).

**(c) Timestamp registers → epoch-ms + one formatter.** New pure module
`packages/nucleus/src/kinds/format.ts` (re-exported from kinds/index.ts, so nucleus's top-level
index — owned by U1 — is untouched; fence-clean, zero imports):
`toEpochMs(value: string | number): number | null` (epoch-ms or ISO-8601 in; null on garbage —
honest, no guessing), `epochMsToIso(ms)`, `formatTimestamp(epochMs, opts?: { now?: number })`
(deterministic UTC — face-stable, locale-free; DOM layers may localize on top), and
`formatAge(epochMs, now)` ("3m ago" — the capture-age chip's helper). Doctrine (module header):
**carriers hold epoch ms** (`Attributed.observedAt` is the anchor); **the RDF wire holds
xsd:dateTime ISO** (derived via `epochMsToIso`; Emporium's dateTime token accepts epoch-ms→ISO);
**display strings come only from the formatter**.

Concrete migrations this slice: `KnobResource.createdAt` `string → number` (epoch ms), with
`apps/rhizome/src/greenhouse-world.ts` parsing ISO→epoch where SPARQL hands it strings and the
turtle/hypertext faces emitting xsd:dateTime via `epochMsToIso`; **`KnobResource.asOf` STAYS a
calendar-date lens parameter** (it is the `?asof` query echo, documented "null = now" — a query,
not testimony; J1 veto honored); greenhouse's preformatted locale `updated` strings become
epoch-ms in the view model + `formatTimestamp` at render; render-dom's local `timeLabel`
delegates to the shared formatter. The mem-* resources' ISO `createdAt` carriers (the as-of
recomputation basis in rhizome memory-world) are **ledgered as follow-up**, not converted this
slice (scope control; "toward epoch-ms" per R4c is satisfied by converging every carrier planter
touches).

Deliberately OUT (flagged for Vera, §11): dim-posture's fate; the GARDEN_DEFAULT dim-skin
`'sophia'` drift.

---

## 5. apps/emporium migration — one adapter-consumption convention survives (R3)

Verified consumers of `@shrubbery/organism/cell/*` outside organism itself:
`apps/emporium/src/shell.ts:42,47` (emporium-store, vocab-views),
`apps/emporium/tests/emporium-shell.integration.test.ts:26-27` and
`emporium-graph-view.integration.test.ts:31-32` (spawn-gardend, emporium-store),
`apps/emporium/package.json:20` (dep). organism is NO-TOUCH → no shims possible → the surviving
convention is **migration**:

1. `emporium-client.ts` + `emporium-store.ts` → `packages/source/src/emporium/` (cell read
   clients, squarely in the package charter).
2. `vocab-views.ts` → `apps/emporium/src/vocab-views.ts` — it imports lit and side-effect-imports
   `@shrubbery/components` (verified :41-47): view code, not transport; apps/emporium is its only
   external consumer (unanimous judge ruling; keeps `@shrubbery/source`'s `"."` charter
   lit-free).
3. Repoint ALL FOUR import sites: `shell.ts` → `@shrubbery/source/emporium` + `./vocab-views.js`;
   **both test files** → `@shrubbery/source/node` (spawn-gardend) + `@shrubbery/source/emporium`
   (the two-file omission that broke two designs' own grep-gates — J1 veto + J3 veto honored);
   `package.json` dep swap (+ description comment cleanup in `main.ts`/`package.json`).
4. A **grep-gate test** (`apps/emporium/tests/no-organism-imports.test.ts`) pins zero
   `@shrubbery/organism` import specifiers in apps/emporium.
5. **Bundled here** (single touch of the test files): the R9 count-derivation fix —
   `emporium-graph-view` derives expected node/edge counts from the live `/emporium` pack read it
   already consumes (classes count + uri-predicate/wire relationship count computed from the same
   vocab data), asserting internal consistency instead of the fossilized 9/14.

After this unit, organism's exports map has zero external consumers (deprecated-in-place,
ledgered); apps/organism stays byte-untouched (`git diff --stat` proves it in acceptance).

---

## 6. Trunk-honesty unit (R9) — lands FIRST

Everything else builds on an honest signal (Design 3's ordering, grafted by J2+J3):

1. `apps/rhizome/src/__tests__/{greenhouse-smoke:80,165; shell-rail-smoke:89,155;
   observatory-shell-smoke:109}` — the `if (!cellUp) { warn; return }` false-green guards become
   `ctx.skip('cell at http://127.0.0.1:7090 is down (NO MOCK)')` (render-smoke/bouquet-smoke
   pattern). Infra-dark shows as skipped, never green.
2. `apps/atelier/tests/vtuber-appearance-mirror.integration.test.ts` `putJson` — capture the 400
   response body and surface it in the `:419` assertion message (drift-vs-regression becomes
   diagnosable; the test may stay red pending that diagnosis — honestly).
3. `apps/storybook/conneg/build-static.test.ts` — invoke build-static via the workspace's own
   tsx, not `npx tsx` (whose 0.21.5 esbuild binary clashes with hoisted 0.28.1), so `test:all`
   measures product truth.
4. Root `package.json`: `"test:all": "pnpm -r --no-bail run test:run"` — one red workspace can no
   longer hide the rest (the single sanctioned root-scripts edit).
5. Resolve the pre-existing uncommitted `pnpm-lock.yaml` modification (inspect, then commit or
   `checkout --`) before any dependency work.

(The emporium count-derivation piece of R9 is bundled with the migration in U10 — §5, D9.)

---

## 7. Seed/dump tooling + static/live parity

- `apps/planter/scripts/seed.mts` — real-API load of the 227-triple
  `garden-default.ux.nt` (nucleus already exports `./seed/garden-default.ux.nt`) via
  `@shrubbery/source/node` `createGraphAndSeedUxConfig` / `seedGraphFromNtFile`, against a local
  gardend (manifest discovery) or a gateway graph (`--endpoint/--graph/--auth-mode/--token`;
  seeding is a write → Editor, correctly).
- `apps/planter/scripts/dump.mts` — reads any graph via a TripleSource and writes a **fossil v1
  provenance-headed .nt** (§2.6), so fossils are produced with capture testimony by construction.
- **Static/live byte-parity test** (J2 graft, the anti-W-1a assertion): dump a live seeded graph
  → serve the planter server once from the live source and once from the fossil → each face's
  body bytes are equal except the capture block, whose readAt equals the fossil `capturedAt`. A
  header-stripped fossil is refused with a named error.
- The full `build-static.mts` route×face S3-layout writer (Design 3's zero-backend deployment
  mode) is a **named later slice** — the parity test above is the piece of it that enters this
  slice (D17).

---

## 8. Docs, ledger, memo

`docs/planter/`:
- `README.md` — slice map; the FID-004 green-run record (binary sha + date); no live-read claim
  precedes it.
- `adapters.md` — the **strangle ledger**: organism/atelier frozen copies vs hoisted modules and
  the retirement plan when the swarm lands; the WireWriter echo-parse doctrine (recorded, not
  implemented); the two retired negotiates; the emporium convention supersession; nucleus
  `StoreStatus` 'empty' absorption when the freeze lifts; mem-* ISO timestamp follow-up; JSON-LD
  body-testimony follow-up. Plus the readPath deployment matrix and the authz SEAM citation.
- `deployment.md` — the three deployment shapes in trust order (fossil/CDN · credentialed
  planter-server · SPA+Cognito), the P2.5 docker-desktop gateway runbook (build pn-gateway:dev +
  parser-pool:dev, dev-local.sh, port-forward svc/pn-gateway 8088:80, `pn-dev-token`,
  PN_CORS_ORIGINS note), Cognito clientId-audience pairing. Never claims cloud-proven.
- `eschaton-gateway-read-seam-memo.md` — §2.7's four unlocks + WP5.1 gap, with consumers named.
  Memo, not code (cloud transition is Eschaton's).
- `site-vocab/shrubbery-site.golden.json` — the verified draft, byte-verbatim (Appendix A);
  `site-vocab/rationale.md` — the vocab rationale + the named precondition (the layout
  serializer's `iriFor` instance minting must migrate to URN space, or expose URN aliases, before
  live sites bind).
- `census-notes.md` — FID-004 status note for the census owner (docs/acceptance/** is NO-TOUCH).

---

## 9. Test strategy — no mocks, every increment an organism

- **P0 pure**: nucleus contract tests (triplesOf ↔ parseNT round-trip on the real 227-triple
  seed; taxonomy; compile-level proof TripleSource ⊥ ShrubberyContract); vocab machine-check;
  negotiate tests; kind parity/formatter tests.
- **P1 fossil**: fossil adapter through the conformance suite (seed body + explicit provenance;
  header codec round-trip; refused-without-capturedAt; EMPTY on empty body); planter-server four
  faces in-process via exported `handle()` (storybook precedent) from the fossil source;
  browser-safety import-graph test.
- **P2 gardend**: conformance vs real spawned gardend (COUNT cross-check via `select` ===
  tripleCount; EMPTY on fresh unseeded graph; `unavailable` on killed cell); FID-004 probe;
  planter-server live faces; SPA happy-dom liveread through `@shrubbery/source`; the planter-owned
  Chromium gate (real browser, real cell, real `sparql_update` → poll re-render, kill-cell →
  honest error).
- **Generic**: conformance vs the oxigraph-WASM SPARQL 1.1 Protocol HTTP server; env-gated
  `PLANTER_SPARQL_URL`; purity grep.
- **Hosted**: conformance in BOTH readPaths behind the local path-rewriter (honest header);
  cell-route probe pins `/api/sparql/query` vs fallback; env-gated P2.5 (`PLANTER_GATEWAY_URL`);
  Cognito arm = construction + whenReady gating + honest-401 classification (no cloud creds in
  CI, stated in-file).
- **Suite honesty**: unreachable infra always `ctx.skip`, never early-return green (U0
  establishes repo-wide); `pnpm test:all` (--no-bail) is the aggregate gate; CI expectations are
  per-package (pre-existing atelier red is documented, not hidden).
- **pnpm-lock.yaml rule**: the lockfile is merge-by-regeneration — units that edit any manifest
  list it, and on merge the integrator re-runs `pnpm install` rather than hand-merging; it is not
  treated as a design collision (all other files are single-owner or dependency-ordered).

---

## 10. Decisions taken (register)

| # | Decision | Resolves |
|---|---|---|
| D1 | Consensus winner = **Design 1 (oss-purist)** by Condorcet (beats each rival 2–1) and Borda (4/3/2); used as backbone with all vetoes applied | three different judge winners |
| D2 | Fossil testimony = Design 3 doctrine: `readAt` = capture time from required provenance (fossil v1 header or explicit `{graphIri, capturedAt}`); construction refused otherwise | unanimous graft; vetoes of D1 "readAt may advance" + D2 "load time" |
| D3 | Store status: package-local `SourceStoreStatus` `'idle'\|'loading'\|'ready'\|'empty'\|'error'` in @shrubbery/source — canonical member names kept (`'ready'`, not `'ok'`); nucleus StoreStatus NOT widened | J1 veto ('ok' mint) + J2/J3 vetoes (nucleus widening vs frozen consumers) |
| D4 | Contract SELECT = D1's `select?(sparql): SelectResult` with typed `SourceTerm` (incl. bnode); adapters map the proven `{rows}` envelope via `parseTerm` internally | J1 graft (D2 `query?`) vs J2 veto (`SparqlRows` shape): veto honored, graft's intent (in-contract COUNT/FID-004 reads) preserved |
| D5 | NO new render resource kind, NO `RenderCtx.capture`: testimony = headers set once pre-dispatch + host-seam body blocks (md/html footer, ttl `#` block); `/source` is an app-level route | J1 endorsed D3's render carrier; J3 vetoed it; J2 sided app-level → 2–1 + smaller blast radius + Link-parity invariant untouched |
| D6 | `readPath: 'mcp' \| 'sparql'`; **default `'sparql'`** (Viewer-eligible) for every deployment shape, `'mcp'` (Editor) explicit opt-in; `cellQueryRoute` constructor option (`'/api/sparql/query'` default, `'/api/graphs/query'` fallback) pinned by in-test probe; `readPath` display field lives on a package-level description subtype, not in nucleus | R6; J1 rationale + J3 graft (least privilege); J2's no-Sophia-isms-in-nucleus charter |
| D7 | Hoist scope = consumed surface only: transports, auth, 4 adapters, store, emporium client/store, node spawn/seed/fossil-io. NO WireWriter / CRDT backends / contract assemblies / app-routes / public-shell. WireWriter echo-parse doctrine ledgered, not implemented | unanimous vetoes of D2's wholesale cell copy; J2 graft |
| D8 | `vocab-views.ts` → apps/emporium (lit + components view code); only client/store enter the package, keeping `"."` lit-free | unanimous; vetoes D1's placement |
| D9 | Emporium migration file list COMPLETE (shell.ts, main.ts, package.json, BOTH tests at :26-27/:31-32, grep-gate) and **bundles the count-derivation fix** (files touched once); the rest of R9 lands first in U0 | J1/J3 vetoes of incomplete lists; U0-first (J2/J3 graft) vs once-touch bundling (J3) conflict resolved: split R9 across U0 + U10 |
| D10 | Trunk honesty is U0, incl. the storybook esbuild/tsx fix and lockfile-dirt reset; every other unit depends on it | J1 rationale, J2+J3 grafts |
| D11 | FID-004 probe = its own unit; **structural `depends_on`** of both host units | J2 hard veto; J3's praise of D2's structural edge |
| D12 | ALL negotiate copies retired (rhizome + storybook conneg) into `packages/render/src/negotiate.ts`, superset result `{path, target, query, pinned}` | J2 veto of D2's leave-alive; R5 |
| D13 | No `'push'` anywhere: live adapters declare `'poll'`, fossil `'static'`; `subscribe?` reserved iff `'push'` | unanimous veto (gardend has no RDF change surface) |
| D14 | `packages/nucleus/package.json` single owner = U2 (tokens devDep); TripleSource exported via nucleus index only (no subpath); `format.ts` re-exported through kinds/index.ts so U1 solely owns nucleus's top-level index | J3 veto of D1's parallel manifest edits |
| D15 | Boot dispatch: explicit `adapter` wins; only two inference rules (`static:` → static-nt, cognito → hosted-gateway); otherwise refuse naming the field | J2+J3 grafts of D3; veto of D2's URI-scheme grammar + self-contradicting default |
| D16 | Timestamps: epoch-ms carriers + nucleus `kinds/format.ts` (toEpochMs / epochMsToIso / formatTimestamp / formatAge); `KnobResource.createdAt` → epoch-ms; **`asOf` stays the calendar-date lens echo**; greenhouse locale strings replaced; mem-* ISO carriers ledgered | J1 veto of D1's asOf conversion; R4c "toward epoch-ms" |
| D17 | `build-static.mts` (route×face static build) deferred as a named later slice; the static/live **byte-parity test ships in-slice** (U12) with dump.mts fossil-writer | J2 graft's explicit allowance |
| D18 | Generic SPARQL adapter is pure SPARQL 1.1 Protocol (zero Sophia-isms), proven vs the real-oxigraph-WASM HTTP reference server + env-gated external run; gardend's job-envelope routes belong to the gardend/gateway adapters, not to it | J2/J3 grafts; "generic by proof, not assertion" |
| D19 | Package name `packages/source` (`@shrubbery/source`); node split via explicit `./node` subpath (visible in import statements) | R7 designer's call; all designs converged |
| D20 | Dom-face floor = storybook htmlShell-around-markdown; rhizome-grade Lit SSR, organism/atelier package migration, dated `?asof` lenses, JSON-LD body testimony, mem-* timestamp conversion = **named later slices** in the ledger | R5, R7, scope honesty |

## 11. Open for Vera (register)

1. **The real name** — `planter` is a working placeholder everywhere (code, package name unaffected:
   the npm package is `@shrubbery/source`, but the app dir/docs say planter). Rename is one
   decree away.
2. **Site vocab jurisdiction/pillar** — the draft self-declares `internal-substrate`
   (lex-scotus-core precedent); whether `shrubbery-site` ever becomes a pillar is explicitly
   Vera's, not this draft's.
3. **dim-posture's fate** — retire it from GARDEN_DEFAULT dimensions, or give tokens a
   POSTURE_APPLICATIONS + applier. Currently documentation-only data that silently no-ops.
4. **GARDEN_DEFAULT ↔ tokens value-set drift** — dim-skin `garden|sophia` vs tokens
   `garden|emporium|research|greenhouse`; theme with vs without `system`; dim-stance in tokens
   only. The Theme class comment makes reconciling this a **pre-freeze condition** of the vocab
   golden: it must be resolved before `shrubbery-site` 0.1.0 freezes/registers.
5. **Vocab freeze/registration timing** — the draft is local-only until EA-3; the registration
   block names the exact garden-side artifacts. When (and whether) to land it in garden's
   VOCAB_REGISTRY is Vera's call.
6. **Atelier migration green light** — atelier keeps its stale `src/cell` fork this slice; its
   migration to `@shrubbery/source` rides the organism follow-up slice.
7. **Eschaton memo handoff** — the gateway read-seam memo (tool-level authz, anonymous-Viewer
   grant class, CORS, cacheable GET route, WP5.1 seeding) is drafted for Eschaton; prioritization
   of the four unlocks is a cloud-ownership conversation.

---

## 12. Build units (final)

Dependency graph (U0 is the root; pnpm-lock.yaml is merge-by-regeneration, §9):

```
U0 ──┬── U1 ──── U3 ──┬── U4 ──┬── U5 ─────────┬── U9 ──┬── U11
     ├── U2 ──────────┤        ├── U6          │        ├── U12
     └── U8 ──────────┼────────├── U7 ──── U13─┘        │
                      └────────└── U10                  U13
```

| id | name | depends_on | one-line goal |
|----|------|-----------|----------------|
| U0 | Trunk honesty first (R9 core) | — | ctx.skip guards, atelier 400 capture, storybook tsx fix, test:all, lock dirt |
| U1 | TripleSource contract in nucleus | U0 | §1 interface + helpers + taxonomy, index export, pure tests |
| U2 | Kind reconciliation (R4 a/b/c) | U0 | DISPLAY_KINDS + 3-way parity, data-kind convention + applyKind, epoch-ms + formatter |
| U3 | @shrubbery/source scaffold: transports, fossil, store, conformance kit | U0, U1 | package + boot/auth + McpClient + fossil doctrine + SourceState + suite + browser-safety |
| U4 | gardend-local adapter + node spawn/seed | U3 | generic read(graphIri), select via parseTerm, spawn/seed/fossil-io under ./node |
| U5 | FID-004 convergence probe (WF-D gate) | U4 | 2ce2357 template; cold + post-restart parity; structural gate |
| U6 | Generic SPARQL adapter + oxigraph protocol server | U3 | pure SPARQL 1.1 Protocol, proven vs real engine over real HTTP |
| U7 | Hosted-gateway adapter (dual read path) | U3, U4 | readPath 'sparql' default / 'mcp' opt-in; path-rewriter proof both paths; P2.5 env-gated |
| U8 | negotiate hoist into @shrubbery/render | U0 | superset NegotiationResult; rhizome + storybook conneg migrated; one negotiate survives |
| U9 | planter-server: four faces from one TripleSource | U3, U4, U5, U8 | rhizome-wholesale structure, htmlShell floor, headers-once testimony, empty/error taxonomy, /source |
| U10 | Emporium migration + count derivation + grep-gate | U0, U3, U4 | complete 4-site import flip, vocab-views into app, live-derived counts |
| U11 | planter SPA + real-browser proof | U2, U3, U4, U5, U9 | boot → store → planFor → renderWorkspace; testimony chip; Chromium gate |
| U12 | Seed/dump CLI + static/live byte-parity | U4, U9 | seed.mts, dump.mts fossil writer, anti-W-1a parity test |
| U13 | Vocab draft + docs + memos | U7, U9 | golden verbatim + machine-check, strangle ledger, deployment runbook, Eschaton memo, census notes |

Full file lists and acceptance criteria are in the structured build-unit output accompanying this
document (and are binding). Files not listed for a unit are out of that unit's scope; the R7
NO-TOUCH list applies globally.

---

## Appendix A — Site self-description vocab draft (verbatim golden JSON)

The verified draft, to be committed byte-verbatim as
`docs/planter/site-vocab/shrubbery-site.golden.json` (U13). The machine-check test
(`apps/planter/tests/site-vocab-draft.test.ts`) gates: datatype tokens ∈
`{string, uri, integer, long, float, double, boolean, dateTime}`; every subject rule is a
`{graph_subject}`-rooted template with legal token names; every layout cross-reference is
datatype `uri`; no instance IRIs under any ontology namespace; `registration.registry_name` ===
`name`.

```json
{
    "classes": {
        "ComponentBinding": {
            "comment": "Binds a custom-element tag string to the npm package that vets and ships it. REFERENCES the sux:renderedByComponent convention: layout triples only ever name tags (e.g. 'mn-sidebar-panel'), and this class is the site-level manifest row (tag -> package -> min version -> persistence class) that says where a tag comes from. It NEVER introduces a parallel tag predicate on layout nodes - sux:renderedByComponent stays the sole tag carrier in layout; unknown tags render the honest fallback.",
            "predicates": {
                "site:componentTag": {
                    "datatype": "string",
                    "multi": false,
                    "required": true,
                    "source": "the custom-element tag EXACTLY as it appears in sux:renderedByComponent literals (e.g. 'mn-sidebar-panel')"
                },
                "site:npmPackage": {
                    "datatype": "string",
                    "multi": false,
                    "required": false,
                    "source": "npm package name exporting the vetted @customElement (npm-install-is-vetting in OSS mode)"
                },
                "site:minVersion": {
                    "datatype": "string",
                    "multi": false,
                    "required": false,
                    "source": "minimum package version whose manifest declares this tag"
                },
                "site:persistenceClass": {
                    "datatype": "string",
                    "multi": false,
                    "required": false,
                    "source": "CURIE of the Emporium vocabulary class the component reads/writes (e.g. 'bm:Bookmark'); a CURIE token, not a resolvable IRI, hence string"
                },
                "site:fallbackTag": {
                    "datatype": "string",
                    "multi": false,
                    "required": false,
                    "source": "tag to render when the bound component is not installed; absence means the host's generic unknown-tag fallback"
                }
            },
            "rdf_types": [
                "site:ComponentBinding"
            ],
            "subject_rule": "{graph_subject}:projection:site:component-binding:{localId}",
            "source_kind": "current-state",
            "identity_kind": "logical-id",
            "store_mode": "materialize",
            "store_target": "projection:site",
            "enforcement": "halt",
            "reconciliation_strategy": "codeBacked",
            "dispatch_mode": "current-state-materialize"
        },
        "ContentSource": {
            "comment": "NEW (no sux: precursor). A triple store a planter host reads a site from - the RDF twin of the TripleSource sub-contract and the {endpoint, graph, auth} boot config. Liveness is declared, never assumed: push | poll | static. Read testimony mirrors the kernel's Attributed<T> naming (packages/nucleus/src/kinds): site:observer ~ Attributed.observer, site:readAt ~ Attributed.observedAt / turnover readAt (epoch-ms; the dateTime token accepts epoch-ms and canonicalizes to ISO). tripleCount 0 is the first-class EMPTY store status - honest data, never an error and never a fallback config.",
            "predicates": {
                "site:endpoint": {
                    "datatype": "uri",
                    "multi": false,
                    "required": true,
                    "source": "base URL of the store: gardend loopback apiUrl, hosted-gateway base (e.g. https://api.canary.sophia-labs.com), or a generic SPARQL endpoint"
                },
                "site:graphId": {
                    "datatype": "string",
                    "multi": false,
                    "required": true,
                    "source": "the graph id passed to the adapter (the {graph} of the three-fact bootstrap)"
                },
                "site:graphIri": {
                    "datatype": "uri",
                    "multi": false,
                    "required": false,
                    "source": "the named graph actually read, e.g. urn:mnemosyne:local:graph:{id}:ux:config (uxConfigGraphIri)"
                },
                "site:authMode": {
                    "datatype": "string",
                    "multi": false,
                    "required": true,
                    "source": "discriminant of the planter boot auth union: 'dev' | 'cognito' | 'none' (never carries the secret itself)"
                },
                "site:liveness": {
                    "datatype": "string",
                    "multi": false,
                    "required": true,
                    "source": "adapter-declared liveness class: 'push' | 'poll' | 'static' (gardend/gateway MCP reads are 'poll' today; the .nt fossil adapter is 'static')"
                },
                "site:readAt": {
                    "datatype": "dateTime",
                    "multi": false,
                    "required": false,
                    "source": "epoch-ms of the last completed read - the TripleSource testimony field (mirrors Attributed<T>.observedAt / turnover readAt); feeds the capture-age chip"
                },
                "site:observer": {
                    "datatype": "string",
                    "multi": false,
                    "required": false,
                    "source": "who performed the read (mirrors Attributed<T>.observer)"
                },
                "site:tripleCount": {
                    "datatype": "long",
                    "multi": false,
                    "required": false,
                    "source": "parsed-body triple count of the last read; 0 = first-class EMPTY status (never trust the envelope quadCount)"
                }
            },
            "rdf_types": [
                "site:ContentSource"
            ],
            "subject_rule": "{graph_subject}:projection:site:content-source:{localId}",
            "source_kind": "current-state",
            "identity_kind": "logical-id",
            "store_mode": "materialize",
            "store_target": "projection:site",
            "enforcement": "halt",
            "reconciliation_strategy": "codeBacked",
            "dispatch_mode": "current-state-materialize"
        },
        "PackProvenance": {
            "comment": "NEW (no sux: precursor). A sha-pinned record of which Emporium vocabulary pack version a site was built against. Naming deliberately reuses the wardrobe artifact-reference and testimony conventions (sux:artifactId / sux:contentSha / sux:witness in wardrobe-rdf.ts) and the Emporium golden discipline (the served golden's pinned sha256 IS the contentSha; version bumps mean a new golden + new sha). A site that renders persistence-class-bound components can prove which contract bytes it trusted.",
            "predicates": {
                "site:packName": {
                    "datatype": "string",
                    "multi": false,
                    "required": true,
                    "source": "the Emporium pack name exactly as registered (VOCAB_REGISTRY row / golden 'name' field, e.g. 'shrubbery-site')"
                },
                "site:packVersion": {
                    "datatype": "string",
                    "multi": false,
                    "required": true,
                    "source": "the pack's semver as served at /emporium/vocab/{name}/{version}"
                },
                "site:contentSha": {
                    "datatype": "string",
                    "multi": false,
                    "required": true,
                    "source": "sha256 of the golden bytes (equals the served ETag; wardrobe contentSha naming)"
                },
                "site:artifactId": {
                    "datatype": "string",
                    "multi": false,
                    "required": false,
                    "source": "cell artifact id when the pack bytes are also archived as a graph artifact (wardrobe artifactId naming)"
                },
                "site:witness": {
                    "datatype": "string",
                    "multi": false,
                    "required": false,
                    "source": "who attests this pin (wardrobe witness naming - testimony, defeasible, two witnesses may disagree)"
                },
                "site:pinnedAt": {
                    "datatype": "dateTime",
                    "multi": false,
                    "required": false,
                    "source": "epoch-ms when the pin was taken"
                },
                "site:vocabUrl": {
                    "datatype": "uri",
                    "multi": false,
                    "required": false,
                    "source": "where the golden is served, e.g. {endpoint}/emporium/vocab/{name}/{version}"
                }
            },
            "rdf_types": [
                "site:PackProvenance"
            ],
            "subject_rule": "{graph_subject}:projection:site:pack-provenance:{localId}",
            "source_kind": "current-state",
            "identity_kind": "logical-id",
            "store_mode": "materialize",
            "store_target": "projection:site",
            "enforcement": "halt",
            "reconciliation_strategy": "codeBacked",
            "dispatch_mode": "current-state-materialize"
        },
        "Route": {
            "comment": "Binds a URL path to the dim-app machinery that planFor already executes. sux: PRECURSORS REFERENCED, NEVER REDEFINED: site:appValue points at the dim-app config-value node (typed sux:ConfigValue, whose sux:literalValue planFor reads to select appRootRegions[app]); site:appRootEntry points at the node typed sux:AppRootEntry for that app. Both references use the URN-space instance-IRI convention (see pack comment) - never IRIs under http://sophia.ai/ux#. This class adds NO region model and NO executor of its own - planFor is the executor; Route only says which paths reach which app value.",
            "predicates": {
                "site:path": {
                    "datatype": "string",
                    "multi": false,
                    "required": true,
                    "source": "URL path pattern the host serves this route at (e.g. '/', '/emporium')"
                },
                "site:label": {
                    "datatype": "string",
                    "multi": false,
                    "required": false,
                    "source": "human-readable route name"
                },
                "site:appValue": {
                    "datatype": "uri",
                    "multi": false,
                    "required": true,
                    "source": "the dim-app config-value node (typed sux:ConfigValue) whose sux:literalValue planFor consumes - STRUCTURAL, never a setAttribute; addressed by its URN-space instance IRI keyed on sux:localId (e.g. urn:mnemosyne:local:graph:{graph_id}:ux:node:val-app-garden), NEVER by an IRI under http://sophia.ai/ux#"
                },
                "site:appRootEntry": {
                    "datatype": "uri",
                    "multi": false,
                    "required": false,
                    "source": "the app-root-entry node (typed sux:AppRootEntry) binding the app to its root region set, addressed by its URN-space instance IRI keyed on sux:localId (never under http://sophia.ai/ux#); absent for the default/garden spine, which falls back to rootRegions"
                },
                "site:surface": {
                    "datatype": "uri",
                    "multi": false,
                    "required": false,
                    "source": "the site:Surface instance this route lands on - an IRI minted ONLY by the Surface subject rule, under {graph_subject}:projection:site:surface:{localId} in the declared graph projection; examples and implementation must never mint it under http://sophia.ai/ux# (or any ontology namespace)"
                },
                "site:order": {
                    "datatype": "integer",
                    "multi": false,
                    "required": false,
                    "source": "presentation order among sibling routes (navigation menus)"
                }
            },
            "rdf_types": [
                "site:Route"
            ],
            "subject_rule": "{graph_subject}:projection:site:route:{localId}",
            "source_kind": "current-state",
            "identity_kind": "logical-id",
            "store_mode": "materialize",
            "store_target": "projection:site",
            "enforcement": "halt",
            "reconciliation_strategy": "codeBacked",
            "dispatch_mode": "current-state-materialize"
        },
        "Surface": {
            "comment": "A navigable presentation of an existing sux: layout. sux: PRECURSORS REFERENCED, NEVER REDEFINED: site:workspace points at the node typed sux:Workspace; site:entryRegion / site:presentsPanel point at nodes typed sux:Region / sux:Panel - all addressed by URN-space instance IRIs per the pack-comment convention, never by IRIs under http://sophia.ai/ux#. All layout facts (hasRegion, childRegion, order, dockState, renderedByComponent, ...) stay in the sux: layout graph - a Surface carries zero layout semantics of its own, only the site-level naming and entry-point selection over layout that already exists.",
            "predicates": {
                "site:label": {
                    "datatype": "string",
                    "multi": false,
                    "required": true,
                    "source": "human-readable surface name (what navigation shows)"
                },
                "site:workspace": {
                    "datatype": "uri",
                    "multi": false,
                    "required": true,
                    "source": "the workspace node (typed sux:Workspace) this surface presents - a reference into the :ux:config layout addressed by its URN-space instance IRI keyed on sux:localId (e.g. urn:mnemosyne:local:graph:{graph_id}:ux:node:workspace-garden-default), NEVER by an IRI under http://sophia.ai/ux#"
                },
                "site:entryRegion": {
                    "datatype": "uri",
                    "multi": false,
                    "required": false,
                    "source": "the region node (a node already typed sux:Region in the layout graph) where navigation enters, addressed by its URN-space instance IRI keyed on sux:localId (never under http://sophia.ai/ux#)"
                },
                "site:presentsPanel": {
                    "datatype": "uri",
                    "multi": true,
                    "required": false,
                    "source": "zero or more panel nodes (typed sux:Panel) this surface foregrounds, each addressed by its URN-space instance IRI keyed on sux:localId (never under http://sophia.ai/ux#); visibility intent only - dockState/defaultVisible remain layout facts"
                }
            },
            "rdf_types": [
                "site:Surface"
            ],
            "subject_rule": "{graph_subject}:projection:site:surface:{localId}",
            "source_kind": "current-state",
            "identity_kind": "logical-id",
            "store_mode": "materialize",
            "store_target": "projection:site",
            "enforcement": "halt",
            "reconciliation_strategy": "codeBacked",
            "dispatch_mode": "current-state-materialize"
        },
        "Theme": {
            "comment": "A site's default appearance, expressed ONLY as references into the dim-theme / dim-skin machinery. sux: PRECURSORS REFERENCED, NEVER REDEFINED: dimension predicates point at nodes typed sux:ConfigDimension; default-value predicates point at nodes typed sux:ConfigValue - all addressed by URN-space instance IRIs per the pack-comment convention, never by IRIs under http://sophia.ai/ux#. Their sux:appliesAttribute literals (e.g. '[data-theme=dark]', '[data-skin] absent (removeAttribute)') are carried VERBATIM on those nodes and are NOT restated here. This class deliberately holds no value lists: packages/tokens has already drifted from GARDEN_DEFAULT (skin garden|sophia vs garden|emporium|research|greenhouse; theme with vs without 'system'), and restating values would mint a third divergent copy. Reconcile that drift BEFORE this golden freezes.",
            "predicates": {
                "site:label": {
                    "datatype": "string",
                    "multi": false,
                    "required": false,
                    "source": "human-readable theme name"
                },
                "site:themeDimension": {
                    "datatype": "uri",
                    "multi": false,
                    "required": true,
                    "source": "the config-dimension node (typed sux:ConfigDimension) for theme (dim-theme) whose sux:hasValue set is the authoritative value list, addressed by its URN-space instance IRI keyed on sux:localId (e.g. urn:mnemosyne:local:graph:{graph_id}:ux:node:dim-theme), NEVER by an IRI under http://sophia.ai/ux#"
                },
                "site:skinDimension": {
                    "datatype": "uri",
                    "multi": false,
                    "required": false,
                    "source": "the config-dimension node (typed sux:ConfigDimension) for skin (dim-skin), addressed by its URN-space instance IRI keyed on sux:localId (never under http://sophia.ai/ux#)"
                },
                "site:defaultThemeValue": {
                    "datatype": "uri",
                    "multi": false,
                    "required": false,
                    "source": "a config-value node (typed sux:ConfigValue) under dim-theme, addressed by its URN-space instance IRI keyed on sux:localId (never under http://sophia.ai/ux#); its verbatim sux:appliesAttribute documents the DOM hook - stores remain the sole appliers"
                },
                "site:defaultSkinValue": {
                    "datatype": "uri",
                    "multi": false,
                    "required": false,
                    "source": "a config-value node (typed sux:ConfigValue) under dim-skin, addressed by its URN-space instance IRI keyed on sux:localId (never under http://sophia.ai/ux#); same verbatim-appliesAttribute discipline"
                }
            },
            "rdf_types": [
                "site:Theme"
            ],
            "subject_rule": "{graph_subject}:projection:site:theme:{localId}",
            "source_kind": "current-state",
            "identity_kind": "logical-id",
            "store_mode": "materialize",
            "store_target": "projection:site",
            "enforcement": "halt",
            "reconciliation_strategy": "codeBacked",
            "dispatch_mode": "current-state-materialize"
        }
    },
    "comanaged_forbidden": [
        "doc:",
        "mnemo:",
        "nfo:"
    ],
    "comment": "Shrubbery site self-description vocabulary (DRAFT, working name per the planter slice - the real name is Vera's decree). A site = a graph: sux: layout triples plus these site: triples plus a three-fact bootstrap {endpoint, graph, auth}. This pack EXTENDS-BY-REFERENCE, never redefines: Surface/Route/Theme/ComponentBinding point at existing layout nodes typed by sux: classes (Workspace/Region/Panel, dim-app/dim-theme/dim-skin ConfigDimension/ConfigValue, AppRootEntry, the renderedByComponent tag convention) as datatype uri; only ContentSource and PackProvenance are genuinely new. Instance subjects live in {graph_subject}:projection:site URN space - NEVER inside the site: or sux: ontology namespaces (the sux: iriFor instance-minting smell is deliberately not inherited). The same rule governs REFERENCES: every uri-datatype site: predicate that points at a layout node addresses it by a URN-space instance IRI - convention urn:mnemosyne:local:graph:{graph_id}:ux:node:{localId}, keyed by the node's sux:localId - never by an IRI under http://sophia.ai/ux#; the layout serializer's iriFor instance minting must migrate to this URN space (or expose URN-space aliases) before live sites bind, a NAMED precondition of this pack, not a silent assumption. Jurisdiction intent: internal-substrate (lex-scotus-core precedent); whether it ever becomes a pillar is Vera's call, not this draft's. Local-only until EA-3 publication; the 'registration' block below is the pack's own statement of the code-side artifacts EA-3 registration requires.",
    "description": "Self-description vocabulary for Shrubbery sites: Surface (a named presentation of an existing sux: workspace), Route (path -> dim-app value; planFor is the executor), Theme (references into dim-theme/dim-skin, no restated value lists), ComponentBinding (tag -> npm package manifest over the sux:renderedByComponent convention), ContentSource (endpoint/graph/auth/liveness with readAt-as-testimony, the TripleSource twin), and PackProvenance (sha-pinned Emporium pack pins with wardrobe artifactId/contentSha/witness naming).",
    "name": "shrubbery-site",
    "namespaces": {
        "rdf": "http://www.w3.org/1999/02/22-rdf-syntax-ns#",
        "site": "http://sophia.ai/site#",
        "sux": "http://sophia.ai/ux#",
        "xsd": "http://www.w3.org/2001/XMLSchema#"
    },
    "primary_prefix": "site",
    "registration": {
        "comment": "Registration contract for when this pack lands in garden (EA-3 discipline). The registry name below MUST equal this golden's 'name' field exactly (registry_names_match_golden_names). The sha256 constant cannot be embedded here (it hashes these very bytes); it is pinned in code at land time and equals the served ETag. Extra top-level keys are tolerated by the golden deserializer (no deny_unknown_fields) and the served body is byte-verbatim, so this block travels with the contract.",
        "registry_name": "shrubbery-site",
        "registry_row": "(\"shrubbery-site\", SHRUBBERY_SITE_GOLDEN_JSON, SHRUBBERY_SITE_GOLDEN_SHA) in VOCAB_REGISTRY, garden/src-tauri/src/emporium/vocabs.rs",
        "embedded_bytes": "SHRUBBERY_SITE_GOLDEN_JSON = include_str!(\"vocabs/shrubbery-site.golden.json\") - served byte-verbatim, never re-serialized",
        "sha_constant": "SHRUBBERY_SITE_GOLDEN_SHA = sha256 of the on-disk golden bytes (recompute with: shasum -a 256 src/emporium/vocabs/shrubbery-site.golden.json), pinned at land time; equals the served ETag",
        "sha_pin_test": "embedded_shrubbery_site_sha_is_pinned",
        "jurisdiction": {
            "name": "shrubbery-site",
            "public_jurisdiction": "internal-substrate",
            "canonical_ontology": "internal-substrate",
            "canonical_pack": "shrubbery-site",
            "status": "internal-substrate",
            "slug_aliases": [],
            "compatibility_aliases": []
        },
        "served_at": "/emporium/vocab/shrubbery-site/{latest|version} (+ /emporium/vocabs catalog row), ETag = sha, ?format=/Accept negotiation",
        "invariants": [
            "registry name equals this golden's 'name' field exactly (registry_names_match_golden_names)",
            "NOT a fourth canonical-public pillar - lex-scotus-core precedent (vocabs.rs jurisdiction comment); the pillar question is explicitly Vera's, not this registration's",
            "version bump = new golden + new sha; the served body is the embedded bytes verbatim"
        ]
    },
    "title": "Shrubbery Site Self-Description Vocabulary",
    "version": "0.1.0",
    "write_target": "projection:site"
}
```

---

## Appendix B — Veto compliance ledger

Every judge veto, and how this design honors it (none is rebutted):

| Veto (judge) | Compliance |
|---|---|
| D2 wholesale `src/cell` full-contract copy (J1, J2, J3) | §2.2: hoist trimmed to consumed surface; no WireWriter/CRDT/assemblies/app-routes/public-shell |
| D2 endpoint-scheme boot dispatch + self-contradicting readPath default (J1, J2, J3) | §2.4: explicit `adapter` wins, two inference rules, refuse-ambiguous; §2.7: 'sparql' default everywhere |
| D1 vocab-views inside @shrubbery/source (J1) | §5: vocab-views → apps/emporium |
| D1 `'ok'` store status mint (J1) | §1.2: canonical member names, package-local `'empty'` only |
| D2 nucleus StoreStatus widening (J2, J3) | §1.2: nucleus untouched; absorption ledgered |
| D1 `asOf` → epoch-ms (J1) | §4c: `createdAt` only; `asOf` stays the lens echo |
| D1/D2 static readAt = load time (J1, J2, J3) | §2.6: capture-time-or-refuse (fossil v1 header) |
| D3 U3 grep-gate with incomplete file list (J1); any migration omitting the two test files (J3) | §5 item 3: both test files in the unit's file list |
| Any 'push' claim this slice (J1) | §1.1/§2: all live adapters 'poll'; subscribe reserved |
| D2 closed SourceKind union (J2) | §1.1: `kind` open string |
| D2 leave-storybook-negotiate-alive (J2) | §3.2: both copies retired |
| Live-read claims before FID-004 green (J2) | §3.4/D11: structural depends_on for U9/U11 |
| D2 SparqlRows raw-string contract shape (J2) | §1.1/D4: SelectResult + typed SourceTerm |
| D1 U1/U8 parallel nucleus package.json collision (J3) | D14: single owner = U2; no subpath |
| D3 site-source render kind + RenderCtx.capture (J3) | D5: app-level /source; headers-once + host-seam blocks |
| D3 U6 20-file mega-unit / U5-U6 double-edits (J3) | §12: negotiate hoist (U8), server (U9), SPA (U11) split along the D2 seam; no unit double-edits render switch files (none are touched at all) |
| D1 'deps: nucleus only' vs hosting lit code (J3) | §2.1/§5: `"."` is nucleus-only for real; vocab-views out |
